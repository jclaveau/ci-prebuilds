{
  "_comment": [
    "Per-row ceilings on the alpine/official ratio, enforced by",
    "assert-perf-budgets.py. These are REGRESSION ALARMS, not parity targets:",
    "the goal is <= 1.00, the budget is where a build has gone visibly wrong.",
    "",
    "A budget is set from the 99th percentile of what a SINGLE probe shot reads,",
    "because that is what this gate sees: the inline probe runs each browser",
    "once per target and divides. It is NOT set from the median ratio. Sizing a",
    "budget off the median produces a gate that fails on healthy builds.",
    "",
    "Seeded from 90 paired shots: perf-probe runs 34289013835 / 34289022039 /",
    "34289030252, three browsers x n=10, drawn across four runner CPU models",
    "(EPYC 7763, EPYC 9V74, Xeon 6973P-C, Xeon 8370C). Ratios are only",
    "comparable within one job, so each shot is paired to its own same-runner",
    "control.",
    "",
    "Two populations, and the difference matters when reading a green run:",
    "",
    "  TIGHT ROWS - dispersion under ~10%, so the budget tracks real",
    "  performance and a breach is a real regression. locator_click, int_math,",
    "  libm_fmod, and every webkit row except eval_rtt/click_force. int_math read",
    "  5-9x when chromium shipped with DCHECKs on; this is the sharpest detector",
    "  we have.",
    "",
    "  DISPERSION-BOUND ROWS - single-shot spread exceeds the signal, so the",
    "  ceiling is set by noise and only catches a catastrophe. firefox launch",
    "  (median 0.97, p99 2.01), firefox js_alloc (median 1.20, p99 2.18),",
    "  chromium screenshot (median 1.00-1.26 by CPU, p99 1.82). Do NOT read",
    "  these budgets as an allowance: firefox launch is AT parity and its 2.15",
    "  ceiling is measurement noise, not a target. Repeats barely help - n=5",
    "  only moves firefox launch's p99 from 2.01 to 1.72, at five times the",
    "  probe cost - so the gate stays at n=1 and these rows stay loose.",
    "",
    "Two rows are CPU-model-dependent rather than noisy, which is why their",
    "budgets look wide: chromium layout reads 1.27 on EPYC 9V74 against",
    "1.59-1.62 on EPYC 7763, and chromium screenshot inverts it, 1.26 on 9V74",
    "against 1.00 on 7763. Both are reproducible across replicate jobs.",
    "",
    "A row whose budget must be raised is a finding. Raise it in the same PR that",
    "causes it, so the number moves where a reviewer can see it."
  ],
  "_invalid_cell_comment": [
    "The arms of one draw run SEQUENTIALLY inside a single job. That divides out",
    "the runner's CPU model, which is what it is for, but it does not divide out",
    "a machine that slows down partway through: the control was measured on a",
    "fast slice and one of our arms on a slow one, and the ratio carries the",
    "difference. Run 34504693887 is the worked example. webkit/ubuntu read",
    "int_math 1.15 and libm_fmod 1.14 together while webkit/alpine, same job,",
    "same control, read 1.007 and 1.000 — on a commit (#209) that changed 41",
    "lines of a dispatch-only probe script and shipped nothing.",
    "",
    "`controls` are the rows that have no legitimate reason to move: int_math is",
    "30M integer ops in-page with no libc call, libm_fmod is one libm call in a",
    "loop. They measure different machinery, so a BUILD change moves one or the",
    "other, never both by the same amount — when chromium shipped DCHECKs on,",
    "int_math read 5-9x and libm_fmod did not move at all. A slow time-slice",
    "moves both, equally, because it is not measuring the build.",
    "",
    "So: both controls above `min_shift`, agreeing within `spread`, and the cell",
    "is declared drifted by their geometric mean. A breach is then excused only",
    "if dividing that shift out brings the row back inside its budget — a row",
    "the drift does not explain still fails, at its adjusted ratio. Nothing is",
    "excused at all once a control passes `hard_ceiling`: 1.5 is far outside any",
    "drift ever measured here and inside the class this gate exists to catch.",
    "",
    "The residual risk is a real regression that slows every row uniformly, which",
    "this would excuse. It is taken deliberately: the alternative is main going",
    "red on a coin flip, which teaches everyone to ignore the colour."
  ],
  "_invalid_cell": {
    "controls": ["int_math", "libm_fmod"],
    "min_shift": 1.05,
    "spread": 1.05,
    "hard_ceiling": 1.5
  },
  "chromium": {
    "click_force": 1.3, "context_page": 1.4, "dom_churn": 1.4,
    "eval_rtt": 1.4, "goto_cold": 1.5, "goto_warm": 1.5,
    "int_math": 1.15, "js_alloc": 1.2, "launch": 1.75,
    "layout": 1.95, "libm_fmod": 1.15, "locator_click": 1.15,
    "screenshot": 1.95
  },
  "firefox": {
    "click_force": 1.15, "context_page": 1.2, "dom_churn": 1.1,
    "eval_rtt": 1.5, "goto_cold": 1.2, "goto_warm": 1.15,
    "int_math": 1.25, "js_alloc": 2.3, "launch": 2.15,
    "layout": 1.3, "libm_fmod": 1.15, "locator_click": 1.15,
    "screenshot": 1.3
  },
  "webkit": {
    "click_force": 1.45, "context_page": 1.1, "dom_churn": 1.15,
    "eval_rtt": 1.45, "goto_cold": 1.15, "goto_warm": 1.15,
    "int_math": 1.15, "js_alloc": 1.4, "launch": 1.2,
    "layout": 1.1, "libm_fmod": 1.2, "locator_click": 1.15,
    "screenshot": 1.1
  }
}
