# PW upstream tests/library + tests/page skip-list for firefox (Alpine/musl,
# PW-patched build).
#
# Format: one PW test full-title regex per line. Blank lines + lines starting
# with `#` are ignored. Joined with `|` into a single `--grep-invert` pattern.
#
# Policy: allow-all default. Add an entry only when a test is known to fail
# for an Alpine/musl-specific reason that cannot be fixed in the artifact.
# Each entry MUST link to an issue (or PR) with reproduction details + a
# disposition: BLOCKER (track to green), DIVERGENCE (won't-fix, document),
# UPSTREAM (waiting on microsoft/playwright fix).
#
# Umbrella tracking issue: jclaveau/ci-prebuilds#TBD ("firefox Alpine
# conformance gaps") — fill in once filed.
#
# Known blocker — Juggler handshake hang (jclaveau/ci-prebuilds#60):
# PW's tests/library/playwright.config.ts hangs at Juggler handshake on our
# musl FF. smoke-firefox-library passes the same binary in 22s, so the
# divergence is in PW's fixture layer (tests/library/playwright.config.ts),
# not the binary. Until #60 is root-caused, the conformance run will time
# out per-test. Re-enable the gate in playwright-alpine-browsers.yml only
# after #60 lands; populate this file with named failures the runner DOES
# emit once it gets past Juggler.
