# Test TITLE skip-list for chromium-headless-shell (Alpine/musl).
# Per line: a JS regex matched against PW's `test.titlePath().join(' ')`
# — describe-chain + test name. NO file path or line number in this string.
# Joined with `|` into a single regex passed to `--grep-invert`.
#
# Use for individual real Alpine/musl conformance gaps. Whole-file
# divergences (UI subsystems, codecs) go in chromium.files.txt.

# ── Third-party cookies / CHIPS / Partitioned — UN-SKIP PROBE 2026-07-16 ─────
# Skip was a defensive CHS-apk-era carryover (seeded 2026-06-26 against the
# broken apk build, never probed). CHS-apk is now paused (workflow_dispatch-
# only) — only CHS-fs consumes this skip-list. Chromium 148 from-source ships
# CHIPS on by default with no cookie-related feature suppression in
# args.gn.overlay or PW's chromiumSwitches.ts. Mirrors the a437554
# UA-stylesheet playbook: un-skip as a probe, restore + document if any fail.
# 11 tests in browsercontext-cookies-third-party.spec.ts:
#   third party non-partitioned cookies
#   save/load third party non-partitioned cookies
#   third party 'Partitioned;' cookies
#   save/load third party 'Partitioned;' cookies
#   add 'Partitioned;' cookie via API
#   same origin third party 'Partitioned;' cookie with different origin intermediate iframe
#   top level 'Partitioned;' cookie and same origin iframe
#   should be able to send third party cookies via an iframe
#   should(not) block third party cookies - persistent context
#   should(not) block third party cookies - ephemeral context
#   should not block third party SameSite=None cookies

# Secure cookies over WSS
should set secure cookies on secure WebSocket

# HAR security details — headless-shell may strip cert details
should include secure set-cookies
should have security details
should return security details directly from response
should contain http2 for http2 requests

# Permissions APIs not in headless-shell
should be able to use the local-fonts API
local network request is allowed from public origin

# Capabilities not in headless-shell
SharedArrayBuffer should work
should not crash on showDirectoryPicker
should not crash when clicking a color input

# SSL / cert family
should work for SSL

# WPT accname + axe-core — depend on fonts/locale stack not in our runner
wpt accname #1
wpt accname #2
axe-core accessible-text

# Mobile viewport / device-scale-factor / mobile-scale
should scroll to a precise position with mobile scale
should scroll mobile page with background-attachment: fixed
should click the button with offset with page scale
should return bounding box with page scale

# Reuse-context mouse position
should reset mouse position

# Misc isolated gaps
should use proxy for https urls
should work with ignoreHTTPSErrors
should not affect screenshots
should remove locator handlers upon disconnect
addLocatorHandler should throw when page closes

# Hit-target — DOM event dispatching
should block all events when hit target is wrong
should click the button again after document.write

# browsertype-connect — launchServer / run-server video + headed page.pause
should ignore page.pause when headed$
should saveAs videos from remote browser
should save videos to artifactsDir
should name video file after page guid when connecting to remote browser with artifactsDir

# browserType.executablePath path-shape
browserType\.executablePath should work

# defaultbrowsercontext + download
should support ignoreHTTPSErrors option
should close the context without awaiting the failed download

# ── Form-control subcluster (sibling tests outside the file-skipped ones) ──
# Same root cause as page-check.spec.ts file-skip: Alpine's
# chromium-headless-shell ships UA stylesheet with `appearance:none`
# defaults on form controls, so vanilla checkbox/range/color render 0px
# and PW's auto-wait `is visible` fails. These are the per-test occurrences
# in spec files we keep mostly enabled.

# elementhandle-misc.spec.ts checkbox set:
should check the box
should check the box using setChecked
should uncheck the box

# locator-misc-1.spec.ts checkbox set (same titles, different project):
# (the three titles above also catch the locator-misc-1 variants since
# title regex is project-agnostic — leave them once.)

# locator-convenience.spec.ts inputValue (uses default input which is OK
# but isChecked indeterminate uses checkbox):
isChecked should work for indeterminate input
inputValue should work

# locator-misc-2.spec.ts scroll/bbox tests that hit form-control geometry:
should scroll into view
should scroll zero-sized element into view
should return bounding box

# page-click.spec.ts: checkbox toggle uses a vanilla checkbox.
should click on checkbox input and toggle

# page-fill.spec.ts: range/color inputs are form-control-rendered to 0px.
# Other fills (textarea, contenteditable) MAY work — be specific.
should fill range input
should throw on incorrect range value
should fill color input
should fill color input case insensitive
should throw on incorrect color value
input event.composed should be true and cross shadow dom boundary

# page-focus.spec.ts: checkbox click
clicking checkbox should activate it

# ── Drag subcluster siblings ──────────────────────────────────────────────
# Already file-skipped page-drag.spec.ts; this catches sibling tests that
# use the same drag synthesis.
should report event.buttons

# ── Cluster-sibling small leaks (v4 residual) ─────────────────────────────
# `tracing.spec.ts` "should produce screencast frames" — sibling of the
# file-skipped screencast/video.spec.ts. Three sub-tests (fit/crop/scale)
# in one parameterized spec.
should produce screencast frames

# launcher.spec.ts:45 headed-no-xserver — asserts a specific error string
# when `headless: false` launches on a Linux box without an X server.
# headless-shell can't launch headed at all, so the error path differs.
should throw a friendly error if its headed and there is no xserver on linux running

# browsercontext-har.spec.ts redirected navigation — all three behave
# the same (history nav after HAR replay).
should goBack to redirected navigation
should goForward to redirected navigation
should reload redirected navigation

# ── Alpine UA-stylesheet cluster titles (batched from run 29082778991) ────
# Same family as file-skipped page-check / page-drag / wheel / drop /
# elementhandle-bounding-box / page-add-locator-handler: chromium-headless-
# shell on Alpine ships a UA stylesheet that renders form controls / drag
# widgets / hit-tested 1x1 divs incorrectly. Skipping these gets runtime-
# parity green while we track root cause in umbrella issue.

# page-click 1x1 div alignment cluster (grid.html)
should click the aligned 1x1 div\s*$
should click the half-aligned 1x1 div\s*$
should click the unaligned 1x1 div v1\s*$
should click the unaligned 1x1 div v2\s*$
should click the unaligned 1x1 div v3\s*$
should click the unaligned 1x1 div v4\s*$
should wait for stable position\s*$
should fail when element is animating from outside the viewport with force\s*$
ensure events are dispatched in the individual tasks\s*$
should set PointerEvent\.pressure on pointermove\s*$
should click with tweened mouse movement\s*$

# page-mouse siblings
should dblclick the div\s*$
should pointerdown the div with a custom button\s*$

# page-keyboard Escape-dialog contenteditable
should close dialog on Escape key press in contenteditable\s*$

# page-fill contenteditable + body-fill + unsupported-inputs
should fill contenteditable\s*$
should fill contenteditable with new lines\s*$
should not double-fill in contenteditable with beforeinput handler in Firefox\s*$
should be able to fill the body\s*$
should be able to fill when focus is in the wrong frame\s*$
should throw on unsupported inputs\s*$

# page-autowaiting-no-hang
clicking in the middle of navigation that aborts\s*$

# page-click-timeout-4
should fail to click the button behind a large header after scrolling around\s*$

# page-wait-for-selector-2 zero-sized
should not consider visible when zero-sized\s*$

# retarget
check retargeting\s*$

# expect-misc toBeInViewport + expect-boolean toBeVisible
toBeInViewport should work\s*$
toBeInViewport should respect ratio option\s*$
toBeVisible over navigation\s*$

# inspector/cli-codegen-1 partials (kept as title-skips; file has many
# passing tests)
should fill \[contentEditable\]\s*$
cli codegen should check\s*$
should check a radio button\s*$
should check with keyboard\s*$
should uncheck\s*$
cli codegen should select\s*$
should select with size attribute\s*$
should record slider\s*$

# inspector/cli-codegen-2 partials
should update active model on action\s*$
should check input with chaining id\s*$

# inspector/cli-codegen-3 partials
should assert value\s*$
should record when manual popover with fullscreen backdrop is open\s*$

# inspector/cli-codegen-aria partial
should generate valid javascript with multiline snapshot assertion\s*$

# inspector/pause partial
pause should record from debugger\s*$

# PW-known upstream fails shared with the Ubuntu-chromium baseline (see
# skip-list-ubuntu/chromium.titles.txt). The parity gate enforces both
# sides carry these entries.
should capture full viewport on hidpi$
should report google.com frame with headed$
