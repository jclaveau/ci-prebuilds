# Test TITLE skip-list for webkit (Alpine/musl, WPE+GTK Branch C).
# Per line: JS regex matched against PW's `test.titlePath().join(' › ')`.
# Joined with `|` into a single regex passed to --grep-invert.

# ── locator-generator single test (WK-only) ────────────────────────────────
# `reverse engineer internal:has-text locators` fails on WK even with the
# chromium bundle (works on FF). Likely PW recorder-vs-webkit codegen edge
# case; other 96 tests in locator-generator.spec.ts pass.
reverse engineer internal:has-text locators

# ── Locale formatting — RECOVERED 2026-07-16 via icu-data-full ──────────────
# Alpine shipped only icu-libs (en-* CLDR); fr-FR/de-DE/ru-RU number+date
# formatting couldn't be produced. Added `icu-data-full` (~30MB full CLDR)
# to the WK runner apk list (build-runner.sh). Removed skips:
#   should format number / should format date / should format number in
#   popups / should be isolated between contexts

# ── Capabilities — media codecs — STRUCTURAL (re-skipped 2026-07-16) ────────
# Un-skip probe (iter 29496096569 shard 7) with gst-plugins-ugly + openh264
# added to the WK runner apks STILL failed all 4. WK's GStreamer media
# pipeline doesn't pick up the decoders from runner apks alone — it needs a
# WebKit build flag / WPE MediaStream backend wiring (audit's predicted risk).
# Not runner-fixable; re-skipped as structural. (Locale part of rank-3 DID
# recover via icu-data-full — kept un-skipped above.)
should play video
should play webm video
should play audio
should not crash on page with mp4

# ── Video / headed remote subcluster ────────────────────────────────────────
should ignore page.pause when headed$
should saveAs videos from remote browser
should save videos to artifactsDir
should name video file after page guid when connecting to remote browser with artifactsDir

# ── File-input / download — STRUCTURAL on WK-musl (bucket-C verdict 2026-07-17)
# Un-skip probe (iter 29565116253) FAILED all 3 — NOT mis-bucketed, genuine
# WK-on-Alpine gaps. They fail across every spec that exercises them
# (browsertype-connect run-server + launchServer, page-set-input-files,
# download.spec.ts); the title grep-invert covers all instances.
#   - upload a folder: directory upload (webkitdirectory file chooser) unsupported
#   - lastModified timestamp: setInputFiles doesn't preserve file mtime on musl
#   - save downloads to artifactsDir: WK download-save path broken on Alpine
should upload a folder
should preserve lastModified timestamp
should save downloads to artifactsDir

# ── Cluster-sibling small leaks (TODO probes) ──────────────────────────────
# Track each pending its own investigation. Don't expand prematurely.
# TODO(probe): webkit-page suite full sweep once skip-list stabilizes

# PW-known upstream fails shared with the Ubuntu-webkit baseline (see
# skip-list-ubuntu/webkit.titles.txt). Parity gate enforces both sides
# carry these entries.
should capture navigation$
