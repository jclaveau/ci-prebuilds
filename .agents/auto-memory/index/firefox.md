# firefox — mozconfig, build passes, FF-side bugs

Full hooks for this area. The routing keys live in `.agents/auto-memory/MEMORY.md`; the memories themselves in `.agents/auto-memory/<slug>.md`.

- [FF's gap vs official was the ALLOCATOR — RESOLVED](project_ff_build_missing_pgo_lto_jemalloc.md) — official links mozjemalloc (`malloc` undefined 0 vs our 1); mimalloc preload in the launch shim takes screenshot 0.69x→0.96x, launch 0.83x→1.02x, eval_rtt→1.00x (PR #110, merged d86baaa). musl string routines INERT despite the same nm shape; PGO not in official either (omni.ja unordered); mimalloc2-insecure beats secure by 10-15%, jemalloc a tie; conformance-firefox is SKIPPED on pull_request so PR-green validates nothing
- [Firefox PNG: bytes fixed, TIME did not follow](project_ff_png_encoder_gap.md) — libpng+zlib must ship as a pair (system libpng brings its own libz) and reaches official's 26801 B byte-identical; but the runtime-probe screenshot row held at 0.68x -> 0.69x across #102, refuting "the gap is encode" — suspect full-viewport capture/readback
- [The hardening arm is VOID — hardening still unpriced](project_ff_hardening_arm_unresolved.md) — `--enable-hardening` already on via aports AND omitting the flag enables it too (deleting the line is a no-op); `--disable-hardening` reached configure yet `.text` came back byte-identical, so ship a `MOZ_HARDENING_CFLAGS` echo before rebuilding
- [FF prebuilt-base pin-only-tag staleness](project_ff_prebuilt_base_pin_only_tag_staleness.md) — base tagged on PW FF rev ONLY; source-only patch to apply-and-build.sh left rev unchanged → iter reused stale base → shipped unpatched ff-latest; fix = content-address base tag on patch-set hash (PR #81); 6-min build = didn't recompile tell
- [FF build is 2-pass cbindgen](project_ff_build_two_pass_cbindgen.md) — first `./mach build` FAILS on cbindgen bare-`[COUNT]`, auto-patches header + retries; mid-build Error 2 is EXPECTED; total ~5-6h; only errors AFTER the patch marker are real
- [FF pageerror crash-family RESOLVED](project_ff_juggler_pageerror_musl_gap.md) — was a Juggler protocol gap (v1.60 omits `location` from Page.uncaughtError, client reads `.location.url` unconditionally); fix threads `location` through Runtime.js/PageAgent.js/Protocol.js; 26 titles recovered, run 29745047841 all-green
- [Firefox shipped 51.8 MB of symbol tables, no DWARF](project_ff_symbol_strip.md) — aports sets `--disable-strip` (distro strips in package(), which we never run); official and our own WebKit both strip, so bundle-dist.sh now does too (04af46d): 52 MiB on disk but only **8.4 MiB compressed** — quote the compressed one
- [Firefox-on-Alpine WIP pipeline lives elsewhere](project_firefox_alpine_wip_pipeline.md) — do NOT add firefox-side checks to this repo's aports tooling/tests; chromium pin pair IS auto-tracked, firefox is not
